# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TikiDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TikiDemo
  
  pod 'Alamofire'
  pod 'RealmSwift'
  pod 'MBProgressHUD'
  pod 'SDWebImage' 

  target 'TikiDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
